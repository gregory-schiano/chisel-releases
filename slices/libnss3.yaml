package: libnss3

slices:
  libs:
    essential:
      - libc6_libs
      - libnspr4_libs
      - libsqlite3-0_libs
    contents:
      /usr/lib/*-linux-*/nss/*:
      /usr/lib/*-linux-*/libfreebl*.so:
      /usr/lib/*-linux-*/libfreebl*.chk:
      /usr/lib/*-linux-*/libnss3.so:
      /usr/lib/*-linux-*/libnssutil3.so:
      /usr/lib/*-linux-*/libsmime3.so:
      /usr/lib/*-linux-*/libssl3.so:
